# 2026-09-14 工作日志

## 使领馆事务看板每日刷新（automation-1786431384487，窗口 9/11-9/14）
- 基础采集：`refresh_diplomatic.sh` fetch 2 候选（均为应排除项）→ build → push `bf5442e`
- AI 补强核验（WebFetch 外交部三栏目 + 8 轮 WebSearch）：
  - ➕ 新增 **evt-consul-006 意大利新任驻广州总领事盛飞（Carlo Sanfilippo）9/7 履新**（使馆官方发布 + 福建省政府英文站交叉印证）
  - 🔄 4 条访华 ongoing→completed：芬兰普伊斯托、牙买加约翰逊·史密斯、津巴布韦奇温加、挪威希尔斯塔德（回填 9/9 六组中挪企业签约）
  - 🔄 **evt-uscn-002 补 APEC 深圳三方会晤悬念**：俄方持续放风、中方 9/11「没有可以提供的信息」、白宫未确认
  - 🗑 删 2 候选：马朝旭会见联合国亚太经社会执行秘书（国际组织）、王毅谈金砖出访（中方出访）
  - 排除定论：布隆迪总统 9/11 抵新德里出席金砖 → 确认未访华
- 部署：build + cp gh-pages + inject_nav 双份 → push `e8113a5`；线上 md5 与本地一致（`a02c3366`）；全部条目 completed（✅×25，无 🟢/📅）
- 看板总量：人事 8 + 领事 6 + 访华 17 + 中美 3 = 34 项
- 经验固化：`boards/diplo.md` 补 4 条（部署链 inject_nav 时机、多边峰会窗口处理、周末窗口特征、总领事「使馆发布」级信源先例）；automation memory 同步

## 关键教训
- `refresh_diplomatic.sh` 不含 inject_nav → 每日 9:21 自动刷新会冲掉 root 的 IRA-NAV，只有 AI 补强提交才重新注入；root 常态有 IRA-NAV + hero-nav 两条导航栏（历史一致状态）
- 多边峰会中方出访不收录，但东道主身份下的「元首互动悬念」应回填 us_china outcomes（不单独立项）

## 国际新闻看板每日刷新（V2.11 归档规则，窗口 9/13-9/14）
- 官方信源：`fetch_us_official.py` 重跑 → **源组丢失坑第 17 次复发**（99→29 条），用 09:50 备份 `/tmp/us-official-backup-0914.json` 恢复，校验 6 源齐全（白宫 26/国务院 39/国防部 27/财政部 3/商务部 1/USTR 3）
- 反爬三源 WebFetch：商务部（最新 9-02）、国防部（9-10）、USTR（9-08）→ 窗口内无新公告，如实空缺
- 白宫 9-13 `Presidential Message on the Anniversary of the Battle of Fort McHenry` 属程序性文告 → 按「无新闻价值不入池」处理（现有正则只覆盖 `National XX Month|Week|Day`，**未覆盖 `Anniversary of the Battle of` 句式，建议后续扩展**）
- RSS 兜底通道（V2.15）7 源 262 条候选 → 窗口内 151 条；CNN/AP 走 WebFetch
- 新增 59 条（collectedAt=09-14，date ∈ {09-13,09-14}）→ 今日版面 **57 条**（2 条同事件多源被去重）
- 归档校验：collectedAt≠今日 **0** / date<昨天 **0** / 历史版面 URL 重复 **0**；字段完整性 100%（缺 title_zh 0、缺摘要 0、模板摘要 0）
- 推送：`49a6138`（主更新）+ `e2add02`（补提交两个 pool 文件）；线上 HTTP 200、275995 字节与本地完全一致
- 飞书：`--today` 只同步 7 条 → 补跑全量 +50 条（去重保护生效）
- 今日版面源分布：SCMP 9 / 路透 8 / NYT 8 / BBC 7 / 卫报 5 / CNN 5 / Politico 5 / 半岛 4 / WaPo 4 / AP 3 / WSJ 1

### 关键新发现（已固化到 boards/intl.md）
- **WebSearch 可直接返回路透官网 URL**（本次命中 `reuters.com/world/europe/trump-says-very-negative-forces-raising-exaggerated-concerns-over-ai-2026-09-13/`）→ 路透条目应改为「先 WebSearch 尝试官网 URL，拿不到再走转载通道」，比逐个找转载更快更优
- 实测合格的转载域名：Yahoo News / Euronext Live / Channel NewsAsia / Livemint / New Straits Times / TechNode Global / Seoul Economic Daily / RBC-Ukraine
- ⚠️ WSJ 候选来自 Google News RSS，**标题常被截断**（如 `...Monumental Crisis for...`）→ 必须二次搜索确认完整标题才可入库，否则有编造风险
- ⚠️ RSS 标题使用弯引号（U+2019/U+201C），与手写直引号不匹配 → 脚本匹配前必须做引号归一化（`’‘ → '`、`“” → "`），本次因此 12 条 URL 匹配失败
- ⚠️ **update-news.sh 全量跑会丢失 repost_from 字段——新增条目同样丢失！**（本次 9 条转载条目全部丢失，前端"转载"标签失效）。修复路径：①改 `data/news-data.json` 按 URL 精确补 `repost_from`；②`sed -n '909,1010p' update-news.sh > /tmp/gen_html_v12.py` 单独跑 GENERATE_HTML_V12 段（勿重跑全量）；③`scripts/inject_nav.py`；④`scripts/check_js_syntax.py`；⑤精确提交 3 文件（news-data.json + 根/gh-pages 两 HTML）

