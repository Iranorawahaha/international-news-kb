# 2026-08-11 工作日志

## 📰 Ira 早报 V3 修复（10:18-10:27）

### 用户反馈
- 删除国际 1、2、4、11、12、13、14（共7条：路透/卫报/美联社/CNN/NYT/白宫×2）
- 删除国内 1、2、6、9、15（共5条：清华副教授/每日读画/伊朗病态依赖/我是人民的勤务员/外交部记者会）
- 修复 #17 后"无人机地狱景观"重复溢出卡片结构
- 修复 #23 后"煤炭工业发展规划"重复溢出卡片结构
- 补全国内要闻缺失摘要（6条）

### 根因分析
1. **模板文件 bug**：scripts/morning_brief_template.html 第 176-180 行（国际区）+ 第 188-191 行（国内区）硬编码了两段预览输出文章（缺 `<div class="card">` 包裹），生成邮件时这些段直接拼到 INTL_CARDS/DOM_CARDS 占位符后面，造成 #17 后出现"幽灵卡片"显示
2. **数据缺失**：联合早报/商务部/发改委信源页面用 urllib 抓不到真实摘要（反爬/页面结构差异），导致"暂无摘要"占位

### 修复
1. **模板清理**：删除两段硬编码 HTML，恢复标准占位符结构 `{{INTL_CARDS}}` / `{{DOM_CARDS}}`
2. **脚本增强**：send_morning_brief.py 新增 `--exclude-intl/--exclude-dom` 参数（按位置索引排除）
3. **数据补全**：通过 WebFetch 获取 6 个页面真实内容，写入 china-news.json 2026-08-11 archive

### 最终产出
- 国际 17→10 篇 | 国内 23→18 篇 | 共 28 条
- 摘要完整率：100%（0 暂无摘要）
- 卡片结构：28 cards 完全规范
- 飞书推送 v3 已发 → 用户 10:31 回复"确认"
- 10:31 已发送邮件 → 2027674540@qq.com ✅
- 提交 7a310fe 已 commit

### 经验固化
- **早报模板/预览分离原则**：模板文件只能含静态 CSS+结构+占位符，禁止任何预览输出残留
- **数据 vs 模板分层**：摘要补全应在数据层（china-news.json）而非模板层，避免下次生成又丢失
- **位置排除 vs 内容排除**：用户反馈基于"位置 N"的删除，用 --exclude-intl/--exclude-dom 比编辑数据更安全
- **WebFetch URL 关键场景**：联合早报 → 重定向到 .com.sg；商务部/发改委/外交部部分页面可访问
- **Unicode 引号坑**：data 文件用 """"（U201C/U201D），Python 字面量字符串必须用 unicode 转义才能匹配

## 📰 Ira 国际新闻看板每日刷新 (09:20-09:55 第六次自动化)

### 执行结果
- 官方源 33 条：白宫5/国务院9/财政部0（curl）+ war.gov 8-10 两条（WebFetch）；14 条英文由 agent 翻译补全 title_zh/summary_zh（100% 中文化，0 导航残留）
- WebFetch 11/11 信源 112 条（昨日 56 有效 + 新增 56），URL 100%
- update-news.sh --auto：440 条/8 天 → git push 6dbcbba → 构建 built → 线上 HTTP 200 与本地 md5 一致
- 今日 8-11 组 40 条；高分：WSJ 95 苹果测试长鑫CXMT / BBC 92 英伟达5000亿美元AI基建 / SCMP 92 中国加入巴西WTO诉美国 / 特朗普相关多条 88-90

### 本次踩坑与修复
1. **飞书同步失败 800030005**：今日 category 出现新值「国际」（5条），分类字段无此选项 → field-update 补选项成功
   - ⚠️ field-update 的 `type` 必须是字符串 `"select"`（传数字 3 报 800010701 Invalid discriminator）
2. **WSJ 反爬升级**：/world、/news/world、sitemap、RSS 全部被 DataDome 拦截，仅能 WebSearch 获知内容；苹果 CXMT 重大新闻用 finwire.io 转载真实 URL 收录（95分）
3. **黄岩岛声明标题含 HTML 实体**（&#8217;等）：翻译匹配需先 html.unescape
4. **前端校验模式**：嵌入数据是 `NEWS_DATA = {...}`（archive 结构）非数组；缺中文 128 条均为 8-04/8-05 历史遗留（即将滚出窗口）
5. 飞书 8-11 入库 40 条与本地完全一致（sync_to_feishu.py 去重修复生效）

### 脚本产出
- scripts/patch_official_0811.py（官方源中文化补丁）
- scripts/gen_webfetch_0811.py（webfetch 合并生成）
- scripts/verify_0811.py（archive 结构校验）

## 📰 Ira 早报自动化

### 09:35 首次执行
- 预览生成：国际 6 篇 / 国内 5 篇
- 飞书确认消息已发 → 等待确认

### 10:12 重写（基于更新后看板数据）
- 预览重生成：国际 17 篇 / 国内 23 篇（共 40 条精选，脚本显示今日新增国际68/国内32）
- 飞书更新确认消息已发 → 等待确认
- 问题：messages-search 缺 search:message scope，改用 chat-messages-list
- 待跟进：auth login 添加 search:message scope（需浏览器授权）

## 脚本改进（两项历史待跟进项完成）

### 1. sync_to_feishu.py 去重查询修复（V1.2.2）
- **根因**：`get_existing_unique_keys()` 使用 `--format csv`，但 lark-cli 1.0.82 不再支持（validation error），导致去重查询一直失败 → 每次同步全量插入 → 飞书重复累积
- **修复**：改用 `--format json` + `--limit 200`/`--offset` 分页，解析 `data.data` 二维数组；用 `fields.index('中文标题'/'来源')` 动态列映射；单元格值兼容 dict/list/标量；网络抖动重试 3 次
- **验证**：查询成功 883 条记录/539 组唯一键（与人工核对一致）；dry-run 445 输入 → 340 重复过滤 → 105 新增预览

### 2. update-news.sh 并发 pid 锁（V1.3.1）
- 在脚本开头（PROJECT_DIR 定义后）增加 `.update-news.lock` 锁：活跃锁拒绝执行（exit 1）、残留锁（PID 不存在）自动清理、EXIT/INT/TERM trap 释放
- 防止 8-10 发生的自动化重试并发覆盖数据问题
- .gitignore 增加 `.update-news.lock`

### 提交
- commit 88f0a21（sync_to_feishu.py / update-news.sh / .gitignore），已 push

### 注意
- dry-run 显示"新增 105 条"是本地 title（双语）与飞书记录键不完全匹配的预估，实际同步由 update --today 增量执行，风险低；若需全量校准可后续核对

---

## 国内新闻看板 09:30 定时刷新（V5.4 首日自动化执行）

### 采集结果
- 脚本 3/3 成功（耗时 3m56s，网络波动偏慢），commit 8b353d6 已推送
- 原始抓取 49 条 | 外交部 7 栏目全部执行（高优先 5 栏目 0 条**属正常**——验证领导人活动最新 07-29/大使任免最新 03-17 确无新文；例行记者会 4 条）| 商务部 6 栏目执行（采纳 3 条）
- 中国政府网·要闻 19 条 boost 全验证通过（≥分类最高档）| 分数分布 100分7条/≥85分 80%

### LLM 质量后处理（手动执行）
- **删除 7 条**：北戴河清华副教授（个人叙事）、"每日读画"座谈会（文化专栏）、联合早报藏南（与央视重复）、"中国经济看速度"（评论稿）、电影产业观察（评论栏目）、数字技改蹲点稿、发改委煤炭规划通知（与 gov.cn 要闻重复）
- **修正分类 1 条**：陆文俊受贿判刑 经贸72→人事任免87
- **补摘要 9 条**：3 条页面 meta 提取真实摘要（陆文俊/商务部打印设备国安调查/商务部反制FCC）+ 3 条联合早报 + 3 条外交部记者会（SSL 故障用事实概括）
- **AI 补强 3 条**（微信搜索发现脚本漏采）：商务部巴西牛肉配额90%（08-11 部委88）、网信办《大型个人信息处理者个人信息保护规定》征求意见稿（08-07 政策80）、工信部 2026 节能降碳诊断服务任务清单（08-10 部委88）
- 最终 45 条 | 今日 5 条 | 7 天覆盖 | 0 重复键 | 0 缺字段
- 提交 b6e7119（质检）+ c6b562a（摘要补全），已推送，线上 200 ✅

### 通道问题记录
- **xwlb 新闻联播 API**：hotspot.api4claw.com TLS 握手失败（curl/urllib/WebFetch 全失败），第三方服务故障，今日未获取
- **tencent-news CLI**：首次安装成功（~/.tencent-news-cli/bin），但 **API Key 未配置**（news.qq.com/exchange 需人工获取）→ 通道不可用，已记录待用户配置
- **git 推送**：沿用 `git -c http.proxy= -c https.proxy= push` 取消本地 7890 代理成功
- cheerio 已装至 node workspace（微信搜索依赖）
